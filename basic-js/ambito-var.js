'use strict'
/**
 * Convertir numeros a strings --> Con la funcion .toString()
 */

function holaMundo(texto) {
    let hola_mundo = 'Texto dentro de la funcion'
    console.log(texto)
    console.log(typeof numero.toString()) // el typeof es para comprobar el tipo de dato
    console.log(hola_mundo)
}

let numero = 12
let texto = 'hola mundo soy una variable global'

holaMundo(texto)
